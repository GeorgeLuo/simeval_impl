# AGENTS

This directory houses the split Chapter 2 instructions. Start with `index.md` for summaries, lean on the table of contents to pick the right segment, and prefer these segmented files over `desc_sim_original.md` unless context is missing. Mindset prompts live in `mindset_prompts/`.
