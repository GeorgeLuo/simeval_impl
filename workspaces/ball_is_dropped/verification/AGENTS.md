# AGENTS

Verifier scripts and captures live here. Treat `verifier.sh` as the entrypoint for checking phenomena against sim-eval streams; keep helper data (sample captures, fixtures) alongside it and refresh captures after plugin changes.
