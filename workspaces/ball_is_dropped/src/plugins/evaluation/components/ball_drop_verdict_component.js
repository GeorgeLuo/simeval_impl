const VerdictComponent = {
  id: "ball_drop.verdict",
  description: "Pass/fail summary for ball drop checks.",
  validate: () => true,
};

module.exports = VerdictComponent;
module.exports.default = VerdictComponent;
