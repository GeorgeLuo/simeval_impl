# AGENTS

Plugin implementations live here. `simulation/` encodes the world behavior; `evaluation/` ingests streams to judge phenomena. Maintain the components/systems subdivision inside each branch.
