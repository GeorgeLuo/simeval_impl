# AGENTS

Simulation workspaces live here, scoped to document revisions. Follow `instruction_documents/desc_sim_impl_code_structure.md` for expected layout (world_desc, memory/phenomena, verification, src/plugins). Work inside the latest workspace unless directed otherwise.
