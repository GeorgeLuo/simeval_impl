# AGENTS

Global memory lives here. Use `ways/` for durable assumptions that shape future decisions, `records/` for timestamped progress logs, and `exceptions/` for explicit deviations from the spec. Review ways before starting work and append new records after tasks; favor new files over editing existing ones.
