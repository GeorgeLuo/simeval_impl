# AGENTS

Use this directory for deterministic helper scripts (e.g., capture, upload). Check `index.md` for what exists before running anything. Add new scripts when a manual step becomes repeatable, keeping paths relative to the repo root.
